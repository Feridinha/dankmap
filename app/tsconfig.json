{
    "extends": "expo/tsconfig.base",
    "compilerOptions": {
        "strict": true,
        "strictNullChecks": true,
        "jsx": "react-native",
        "paths": {
            "@api-types": ["../api/src/responses"]
        }
    }
}
